const fs = require('fs');
const path = require('path');
function readFileEntry (dir) {
    if (!dir) {
        return [];
    }

    // cosnt fs.readFileSync('')
}
console.log(path.join(__dirname, '../src'))

module.exports = {

}